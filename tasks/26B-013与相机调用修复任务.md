# 26B-013 与相机调用修复任务

来源：Gitee `feature/ui-polish-camera` 只读核验。

## P0 测试订单可进入

- [ ] 后端扫码解析对 `production_orders.order_no` 做精确查询；`26B-013` 返回 `PRODUCTION_ORDER`。
- [ ] Android 与后端统一订单类型枚举，禁止 `ORDER_NO`、`LOGISTICS_NO` 和 `PRODUCTION_ORDER` 混用。
- [ ] 扫码成功后进入 `loadOrder("26B-013")`，再进入 `OrderDetailScreen`。
- [ ] 手动输入 `26B-013` 也必须走相同订单查询链路。
- [ ] `/api/v1/orders/material-status` 按订单过滤并返回该订单的设备和物料需求，不返回全量 `materials`。
- [ ] 增加回归测试：扫码解析 26B-013、手动输入 26B-013、订单详情返回 75 条需求、未知订单 404。

## P0 相机调用闭环

- [ ] 在已授权 Android 真机验证 `PreviewView` 出图。
- [ ] 验证权限拒绝、永久拒绝、无摄像头、摄像头被占用、初始化超时均有可操作提示。
- [ ] 验证 `ImageProxy.close()` 覆盖成功、失败、异常和取消路径。
- [ ] 验证切换页面后 CameraX 已解绑且不再发出扫码事件。
- [ ] 验证重试会重新创建预览、分析器和 executor，不复用已关闭对象。
- [ ] 使用包含生产订单号 `26B-013` 的测试二维码验证：相机识别 -> 订单解析 -> 订单详情。
- [ ] 保留手动输入作为相机失败时的可用降级路径。

## P1 验证环境

- [ ] 安装 `material-flow-backend/requirements.txt` 后运行后端测试。
- [ ] 安装匹配的 Android SDK/JDK 后运行 `./gradlew --no-daemon :app:assembleDebug`。
- [ ] 分别记录构建、模拟器和真实相机验证结果；不得用静态代码检查替代真机证据。

## Git 边界

- [ ] 只在物料流转独立工作区提交。
- [ ] 不提交 VPS 数据库、备份、运行日志、主机标识、随机设备编号、token、密码或私钥。
- [ ] 推送前检查 `git diff --name-status origin/main...HEAD` 和禁止路径。
