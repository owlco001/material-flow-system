# 物料流转系统当前实现审查与优化路线

更新时间：2026-09-15
审查分支：`feature/ui-polish-camera`
审查基线：`8351f11`；本轮优化提交：`058e8dd`、`8892aab`、`1f2600b`、`e17a800`、`43f0810`、`6648c2e`、`41430ae`、`5cd2af0`、`a7ecd1c`

## 1. 审查结论

当前系统已具备可运行的内部测试版本，但尚未达到完整 V1 交付门禁。后端核心认证、库存事务、审批隔离、部署初始化和脱敏测试 seed 已有自动化覆盖；Android 已有登录、角色工作台、用户管理和摄像头扫码代码骨架，但离线同步、完整流转闭环、真实摄像头验收和生产部署能力仍不完整。

本审查只读完成，未修改业务源码、VPS 或数据库。

## 2. 模块图（文字版）

```text
Android
  MainActivity
    └─ LogisticsApp（路由/会话门禁）
       ├─ Login / EndpointConfig / AdminActivation
       ├─ Workspace（角色工作台）
       ├─ Scanner（CameraX/ML Kit/手动输入）
       ├─ OrderDetail / MaterialDetail
       ├─ Approval / UserManagement / Audit
       └─ Repository
          ├─ HttpURLConnection + org.json（待后续 Retrofit 迁移）
          ├─ Room offline_operations（含加密边界）
          ├─ OfflineSyncScheduler（WorkManager）
          └─ OfflineSyncWorker（网络约束/批量/重试）

FastAPI
  ├─ Auth / setup / user admin
  ├─ Scan resolve / order detail / workspace
  ├─ Transfer / approval / execution
  ├─ Handover / assembly / exception / location
  ├─ Audit / file upload
  └─ SQLite WAL（测试/当前部署）
```

## 3. 已完成能力

### 3.1 后端

- Argon2id 密码哈希、刷新令牌轮换、会话吊销和登录失败锁定。
- 初始管理员页面引导、一次性初始化、幂等键、首次改密强制策略。
- 库存执行事务、库存版本校验、库存不足回滚、执行幂等。
- 审批人与执行人隔离、ADMIN/WAREHOUSE_ADMIN 权限边界。
- 脱敏、固定、幂等测试 seed：3 个订单、6 个中文机台、8 个中文物料、4 个库位及关联记录。
- 本地后端全套测试：`40 passed`；compileall 和 diff-check 通过。

### 3.2 Android

- 未登录后端地址配置和初始管理员激活页面。
- 登录/冷启动会话恢复与 `mustChangePassword` 路由门禁。
- ADMIN 用户管理入口、中文角色下拉、添加用户表单和返回工作台。
- 已构建并核验过 `0.3.8 / versionCode 9` APK，应用名为“博阳智造”。
- CameraX/ML Kit、手动输入和基础 1500ms 去重代码已存在。
- 离线同步第一切片已接入 WorkManager：唯一任务、联网约束、批量上限、重试上限、409 冲突态和 Keystore 加密边界。
- 生产订单扫码已进入订单详情与物料状态读取链路，支持加载失败重试和返回扫码。
- 装配工工作台已增加“扫码机台码”，可按机台筛选本人装配任务并使用既有开工、进度、完工操作。
- 物料异常提交已增加必填校验、提交中防重复和服务端状态反馈；流转申请保留稳定幂等键并显示 `PENDING_APPROVAL`。

## 4. 必须修复的高优先级缺口

### P0-1：统一扫码和订单枚举

现状（已完成代码切片）：后端扫码和订单查询已冻结枚举，设备码归入 `UNKNOWN`，旧 `ORDER_NO` 请求拒绝；Android 已同步移除运行时 `MACHINE`。

目标：严格使用：

```text
PRODUCTION_ORDER
FLOW_NO
MATERIAL_CODE
LOCATION_CODE
UNKNOWN
```

禁止运行时出现 `MACHINE`、`ORDER_NO`、`LOGISTICS_NO` 等旧值。设备码如需支持，应通过现有业务类型映射为契约允许的资源表达，不新增未冻结枚举。

接口签名：

```python
POST /api/v1/scan/resolve(body: ScanResolveRequest) -> ScanResolveResponse
POST /api/v1/orders/material-status(body: OrderMaterialStatusRequest) -> OrderMaterialStatusResponse
```

验收：后端 `40 passed`，旧枚举返回 400；真实 seed 条码解析仍需部署后回读。

### P0-2：补齐写接口结构化模型和请求头门禁

现状（第一切片已完成）：库位绑定、盘点创建/确认、异常创建及审批已具备结构化模型、角色、UUID 请求 ID、幂等和审计处理；文件上传及其他写接口的统一请求头门禁仍需补齐。

目标：每个写接口使用 Pydantic 请求模型，统一要求：

```http
X-Request-Id: UUID
Idempotency-Key: UUID（幂等写接口）
```

必须覆盖：库位绑定、盘点创建/确认、异常创建/审核、文件上传元数据、交接动作。

禁止通过客户端隐藏替代服务端鉴权；所有写入均需服务端角色校验、审计和幂等记录。

### P0-3：Android 离线同步改为可恢复状态机

现状（第一切片已完成）：Room 离线记录具备加密边界，已接入 WorkManager 自动联网调度、唯一任务、批量和重试约束；物料流转提交已保留稳定幂等键；进程重启和真机恢复仍缺验收证据。

目标：

```text
QUEUED → SYNCING → SUCCEEDED
                 ├→ RETRY_WAIT（网络/5xx）
                 ├→ CONFLICT（409）
                 └→ FAILED（不可重试）
```

固定参数：

- 每批最多 20 条。
- 并发最多 2。
- 自动重试最多 5 次。
- `clientOperationId` 重试不变。
- 409 进入 `CONFLICT`，不得自动改写 payload。
- payload 使用 Android Keystore 派生密钥加密。
- Worker 使用网络约束，进程被杀后可从 Room 恢复。

### P0-4：完成扫码到业务闭环

目标链路：

```text
权限 → PreviewView → CameraX 绑定 → 条码分析
→ 服务端 resolve → PRODUCTION_ORDER
→ 订单详情 → 物料状态 → 流转申请
→ 审批/执行 → 审计
```

保留手动输入和扫码枪适配接口作为降级入口。不得仅以静态 CameraX 代码宣称扫码验收完成。

### P0-5：真机与部署验收

必须实际记录：

- 设备型号、Android 版本、内存档位。
- 首次相机授权、拒绝、永久拒绝、无相机。
- QR、Code 128、Data Matrix。
- 旋转、锁屏、后台恢复和 30 分钟连续扫码。
- 200% 字体、TalkBack、断网/弱网、进程被杀恢复。
- APK 路径、大小、SHA-256、aapt 元数据。
- 后端部署 revision、健康检查、只读业务接口、数据库备份和回滚结果。

## 5. 技术选型及理由

- Android 网络层：优先 Retrofit + OkHttp，减少手写 HTTP 状态和 JSON 解析错误；使用统一错误 DTO 保留 `code/retryable/traceId`。
- Android 序列化：kotlinx.serialization，DTO 与 JSON 字段显式绑定。
- 离线数据库：Room，采用迁移而不是破坏性重建；payload 使用 Keystore 加密后再落库。
- 调度：WorkManager，使用网络约束、唯一工作名和指数退避；业务幂等由服务端最终保证。
- 后端 API：FastAPI + Pydantic，写接口禁止裸 `dict`；保留 SQLite WAL 作为开发/测试实现，生产切换 PostgreSQL。
- 部署：systemd + Uvicorn + Nginx；生产必须 HTTPS，测试 HTTP 仅限受控网络。
- 测试：后端 pytest；Android JVM/Compose 契约测试；MockWebServer；真机专项测试单独记录，不以编译替代。

## 6. 模块依赖约束

```text
UI/Compose
  → ViewModel/StateFlow
    → UseCase/Domain policy
      → Repository interface
        → Retrofit/Room/WorkManager adapters

FastAPI routes
  → Pydantic request/response models
    → authorization/idempotency service
      → transaction service
        → SQLite/PostgreSQL repository
          → audit service
```

禁止：

- Composable 直接发 HTTP。
- 客户端根据数量推导领取/到工位/审批状态。
- 客户端绕过服务端角色判断。
- 业务代码依赖 APK 中的固定公网地址。
- seed 写入密码、Token、真实主机信息或可登录默认账号。

## 6.1 可直接开发的数据结构定义

### 扫码

```python
class ScanType(str, Enum):
    PRODUCTION_ORDER = "PRODUCTION_ORDER"
    FLOW_NO = "FLOW_NO"
    MATERIAL_CODE = "MATERIAL_CODE"
    LOCATION_CODE = "LOCATION_CODE"
    UNKNOWN = "UNKNOWN"

class ScanResolveRequest(BaseModel):
    raw_value: str = Field(min_length=1, max_length=128, alias="rawValue")
    client_operation_id: UUID = Field(alias="clientOperationId")

class ScanResolveResponse(BaseModel):
    type: ScanType
    normalized_value: str = Field(alias="normalizedValue")
    resource_id: str | None = Field(default=None, alias="resourceId")
    display_name: str | None = Field(default=None, alias="displayName")
```

### 离线操作

```kotlin
enum class SyncStatus { QUEUED, SYNCING, SUCCEEDED, RETRY_WAIT, CONFLICT, FAILED }

data class OfflineOperation(
    val id: Long,
    val clientOperationId: String, // UUID，唯一且重试期间不变
    val operationType: String,
    val encryptedPayload: String,
    val status: SyncStatus,
    val retryCount: Int,
    val errorCode: String?,
    val createdAtEpochMs: Long,
    val updatedAtEpochMs: Long
)
```

不允许将密码、access token、refresh token 或完整请求头写入 `encryptedPayload`；payload 只保存业务请求所需字段。

## 6.2 精确接口与函数签名

### Android 离线同步

```kotlin
interface OfflineOperationDao {
    suspend fun insert(operation: OfflineOperation): Long
    suspend fun claimBatch(limit: Int = 20): List<OfflineOperation>
    suspend fun updateStatus(id: Long, status: SyncStatus, errorCode: String?, now: Long)
    suspend fun countByStatus(status: SyncStatus): Int
}

class OfflineSyncScheduler(context: Context) {
    fun enqueue(): Operation
}

class OfflineSyncWorker(appContext: Context, params: WorkerParameters) : CoroutineWorker(appContext, params) {
    override suspend fun doWork(): Result
}
```

约束：`claimBatch` 不得超过 20 条；调度使用固定唯一工作名和 `KEEP`；网络/5xx 未超过 5 次时返回 `Result.retry()`；409 更新为 `CONFLICT` 并返回成功，避免无限重试。

### 后端写接口模型

```python
class LocationBindRequest(BaseModel):
    material_code: str = Field(min_length=1, max_length=64, alias="materialCode")
    location_code: str = Field(min_length=1, max_length=64, alias="locationCode")
    client_operation_id: UUID = Field(alias="clientOperationId")

class StocktakeCreateRequest(BaseModel):
    location_id: UUID = Field(alias="locationId")
    client_operation_id: UUID = Field(alias="clientOperationId")

class ExceptionCreateRequest(BaseModel):
    order_id: UUID = Field(alias="orderId")
    exception_type: str = Field(min_length=1, max_length=64, alias="exceptionType")
    description: str = Field(min_length=1, max_length=2000)
    client_operation_id: UUID = Field(alias="clientOperationId")
```

以上模型是 R02 的目标契约；当前实现仍需将对应裸 `dict` 路由迁移到这些模型后，才能标记 R02 完成。

## 7. 风险与打回项

以下事项在修复前不能标记为完整 V1：

1. 后端继续返回契约外 `MACHINE` 或接受旧 `ORDER_NO`。
2. 写接口继续使用裸 `dict` 或缺少幂等/请求 ID。
3. 以 JVM 构建成功宣称 WorkManager 离线能力已完成真机验收。
4. 以 APK 构建成功宣称真机扫码和断网恢复通过。
5. 以 SQLite 测试部署宣称生产数据库部署完成。
6. 在现有已初始化 VPS 上隐式重置管理员密码或破坏 setup 状态。

## 8. 分阶段交付顺序

```text
P0-1 枚举契约
  → P0-2 写接口模型/权限/幂等
    → P0-3 Android 网络与离线状态机
      → P0-4 扫码业务闭环
        → P0-5 真机与部署验收
          → P1 PostgreSQL/HTTPS/文件与打印完善
```
