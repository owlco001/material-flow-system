# 智慧工厂 更名说明

> 变更日期：2026-09-20
> 基线：`feature/ui-polish-camera` @ `171a0de`
> 版本：Android `0.4.2` → **`0.5.0`**（versionCode 13 → 14）；后端 `0.3.7` → **`0.4.0`**

## 一句话

产品对外名称由「厂内物料流转系统 / 物流流转系统」统一为**「智慧工厂」**，App 名称保持**「博阳智造」**不变。

## 改了什么

| 位置 | 变更前 | 变更后 |
|------|--------|--------|
| Android 开机动画 · 产品名 | 物料流转系统 | 智慧工厂 |
| Android 开机动画 · 标语 | 博阳智造，构建未来 | 博阳智造，构建未来（未变） |
| Android 开机动画 / 登录页 · 副标题 | 厂内物料流转 · 扫码作业平台 | 智慧工厂 · 扫码作业平台 |
| Android 设置页 · 主题注释 | 物流流转系统主题 | 智慧工厂主题 |
| 后端 API 标题（OpenAPI 可见） | 物料流转系统 API | 智慧工厂 API |
| 文档正文（11 个文件） | 物料流转系统 / 物流流转系统 | 智慧工厂 |

App 名称 `app_name` 仍为「博阳智造」，未改。

## 为什么有东西没改（重要）

以下三处**故意保留**原样，不是遗漏：

### 1. Android 包名 `com.company.logistics`

改包名等同于发布一个**全新的 App**：已安装旧版本的设备不会覆盖升级，而是并排出现两个图标，旧包的登录态与离线队列数据无法继承。车间设备现场会出现「两个博阳智造」。

当前取舍：**保留 `logistics` 包名**，换取已装机器可平滑升级。若将来确实需要更换，必须配套：卸载指引 + 数据迁移 + 版本共存期。

### 2. 后端数据路径与环境变量

`MATERIAL_FLOW_DATA`、`/srv/material-flow/data`、`material_flow.db`、`SERVICE_NAME=material-flow` 全部**保持不变**。

这些指向服务器上的**生产数据库与上传目录**。改名而不做迁移脚本，服务重启后会找不到库并新建空库——历史库存、审计记录在界面上看起来就像「全部丢失」。

当前取舍：**只改对外名称，不动数据路径**。参见 `material-flow-backend/app/main.py` 第 28–32 行。

验证结果：加载后 `title=智慧工厂 API`，路由数 66，`DB_PATH=/srv/material-flow/data/material_flow.db`，`SERVICE_NAME=material-flow` —— 三者均未变。

### 3. 文档文件名与指向它们的引用

`docs/` 与 `tasks/` 下的文件名保持不变（如 `物料流转系统-V1-API契约冻结补遗.md`）。因此代码与文档中引用这些文件名的位置（`Models.kt`、`main.py`、`test_contract_acceptance.py`）也**必须保留原名**，否则引用会失效。

正文替换脚本对**反引号包裹的路径引用**做了保护，只改正文产品名、不动路径字面量。

## 为扩展预留的位置

品牌文案已收敛为单一来源，见 `app/src/main/res/values/strings.xml`：

- `app_name` — 博阳智造
- `brand_subtitle` — 智慧工厂 · 扫码作业平台
- `brand_tagline` — 博阳智造，构建未来

后续新增模块若要调整副标题或标语，**只改这一处**，开机动画与登录页会同步生效。页面内不再硬编码品牌文案。

## 顺带修复的问题：手电筒按钮使用 emoji（违反图标规范）

`ScannerScreen.kt` 的手电筒开关用 `"🔦"` / `"💡"` 作为功能图标。emoji 在不同厂商 ROM 上字形差异极大、无法跟随主题着色、描边粗细不可控，作为功能图标不合格。

已改为矢量图标 `ScannerIcons.FlashOn / FlashOff`（按 Material 官方 24dp 网格重建，沿用现有 `material-icons-core` 依赖，**未引入新图标库**），并按底色深浅自动切换图标颜色以保证对比度。图标定义放在已有的 `ScannerComponents.kt` 中（未新建文件，避免影响构建布局）。

修复前后扫描对比：

| 文件 | 修复前真 emoji | 修复后真 emoji |
|------|---------------|---------------|
| `ScannerScreen.kt` | `🔦` `💡`（手电筒按钮） | 无 |
| 其余改动文件 | 无 | 无 |

> 说明：全仓仍有 `☰` `✓` `⚠` 等**排版符号**，它们属等宽字形、不参与彩色 emoji 渲染，且均为基线既有，按规范原意（禁止 emoji 表情作功能图标）合规，未改动。

## 测试验证与一个必须澄清的结论

### Android 单元测试存在**基线既有的**顺序依赖，与本次更名无关

`LogisticsViewModel*` 系列测试存在跨测试类的执行顺序依赖，表现为间歇性失败（约 50% 概率）：

```
LogisticsViewModelUserManagementTest > deleteUserExposesLoadingThenSuccessAndRefreshesList FAILED
LogisticsViewModelAssemblyTest > removingAssemblyMemberUpdatesTaskAndReportsConflict FAILED
LogisticsViewModelAssemblyTest > assignmentFailureClearsLoadingAndExposesRetryMessage FAILED
```

**根因**：所有测试类都按同一模式构造被测对象——

```kotlin
val scope = CoroutineScope(SupervisorJob() + Dispatchers.Unconfined)
val viewModel = LogisticsViewModel(repository, scope)  // init 内启动会话恢复协程
viewModel.login(...)                                    // 未等待恢复协程结束
```

`Dispatchers.Unconfined` 让协程立即执行到首个挂起点；而 fake repository 的方法都是同步返回，于是恢复协程会**一路跑完**并把 `authState` 写成 `Unauthenticated`。若它落在 `login()` **之后**落地，就会覆盖刚写入的 ADMIN 登录态，使后续的角色守卫（`if (role != ADMIN) return`）直接返回、断言失败。

两个条件同时成立才会失败：**多测试类混跑**（JUnit 类顺序不确定）+ **恢复协程恰好后落地**。

**证据链**：

| 运行方式 | 结果 |
|----------|------|
| 纯基线 `171a0de`，多类混跑 3 轮 | **FAILED / SUCCESSFUL / FAILED**（2/3 失败） |
| 单类运行 `*LogisticsViewModelUserManagementTest*` | 5/5 通过 |
| 单类运行 `*LogisticsViewModelAssemblyTest*` | 通过 |
| 仅添加两条**未使用的 import**（零逻辑改动） | 可复现失败 |

**结论**：这是测试自身对 `Dispatchers.Unconfined` 调度顺序的依赖，**在基线代码上原生存在**，并非本次更名引入。排查中一度误判为「改动引入的竞态」，经基线 A/B 对照（改动全部 stash 后同样失败）后证伪并撤回。

**未修复，如实记录。** 正确修法应让测试显式等待会话恢复完成（在同一 Unconfined 调度器上让出执行权并等待恢复流程结束），或改用 `StandardTestDispatcher` + `runTest` 控制虚拟时间。这属于测试基础设施改造，不在本次更名范围内。

### 后端测试

全量运行 `23 failed, 52 passed`。**已 A/B 验证为基线既有**：对 stash 后的基线跑同一套全量测试，结果完全一致（23/52）。

逐文件隔离运行：**28/28 全部通过，0 失败**。失败仅出现在全量混跑，属测试间的共享状态污染（脚手架问题），非产品代码缺陷。

## 遗留问题（更名之前就存在，本次未修）

1. `docs/V1角色与枚举契约冻结决议.md` 第 51 行引用的 `docs/物料流转系统-V1-需求冻结与接口契约.md` **不存在**（实际文件名是 `物料流转系统-V1需求冻结与接口契约.md`，少一个连字符）。
2. `docs/物流流转系统-APP开发进度.md` 第 19–23 行引用 `/root/project_workspace/docs/...` 若干绝对路径，该目录在当前仓库环境**不存在**。
3. Android 包名 `logistics` 与产品名「智慧工厂」语义已不一致，属于技术债。
4. `任务/` 与 `docs/` 下文件名保留旧产品名，仅为维持引用有效；如需一并更名，须同步更新代码中的引用点。
